name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Install and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install backend dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          python -m pytest -q

  build_and_publish_images:
    name: Build and publish Docker images
    runs-on: ubuntu-latest
    needs: test
    if: success()
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        run: |
          IMAGE=ghcr.io/${{ github.repository }}/quishguard-backend:latest
          docker build -t $IMAGE ./backend
          docker push $IMAGE

      - name: Build and push frontend image
        run: |
          IMAGE_F=ghcr.io/${{ github.repository }}/quishguard-frontend:latest
          docker build -t $IMAGE_F ./frontend
          docker push $IMAGE_F

  deploy_frontend_pages:
    name: Deploy frontend to GitHub Pages
    runs-on: ubuntu-latest
    needs: test
    if: success()
    steps:
      - uses: actions/checkout@v4

      - name: Set API_URL in frontend
        run: |
          echo "Replacing API_URL in frontend/index.html with BACKEND_URL secret if set"
          if [ -n "${{ secrets.BACKEND_URL }}" ]; then
            sed -i "s|let API_URL = 'http://localhost:8000';|let API_URL = '${{ secrets.BACKEND_URL }}';|" frontend/index.html || true
            echo "Replaced API_URL with secret BACKEND_URL"
          else
            echo "No BACKEND_URL secret set; leaving default API_URL"
          fi

      - name: Deploy Frontend to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend
          publish_branch: gh-pages
